# Baked into the production bundle by `vite build`.
# The deployed React app calls this backend for /upload, /process, /chat, etc.
VITE_API_URL=https://video-summarizer-with-rag-backend1.onrender.com
